import React from 'react';
import { History } from './types';
declare type Props = {
    history: History;
    timeout?: number;
};
export declare const Router: React.FC<Props>;
export default Router;
